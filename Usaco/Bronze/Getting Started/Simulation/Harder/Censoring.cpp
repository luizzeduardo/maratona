#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
    freopen("censor.in", "r", stdin);
    freopen("censor.out", "w", stdout);

    string s, t;
    cin >> s >> t;
    int n = s.size();
    set<char> censura;
    int total = 0;
    for(int i=0; i<n; i++){

    }
}